<div class="text-center user-info-top">
  <% if current_user.avatar.attached? %>
    <%=  image_tag(url_for(current_user.avatar.variant(resize: "180x180"))) %>
  <% end %>
  <h1 class="aside-name text-center">
    <%= current_user.name %>
  </h1>
</div>
